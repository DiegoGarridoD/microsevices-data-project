server:
  port: 8080
  
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: hu1-service
          uri: lb://hu1-service
          predicates:
            - Path=/hu1/**