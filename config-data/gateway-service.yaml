server:
  port: 8080
  
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: hu1-service
          uri: http://localhost:8001
          predicates:
            - Path=/hu1/**